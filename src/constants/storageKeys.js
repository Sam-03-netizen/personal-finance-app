/** localStorage keys — keep namespaced to avoid clashes with other apps */
export const STORAGE_KEYS = {
  TRANSACTIONS: 'personal-finance-app:transactions',
  BUDGET: 'personal-finance-app:budget',
}
